# --- Installing on a fresh host ---------------------------------------------
#     python -m venv .venv
#     .venv\Scripts\activate
#     pip install -r requirements.txt

# --- Then, before the first production wave ---------------------------------
#     pip freeze > requirements.lock.txt

# --- Configuration ----------------------------------------------------------
python-dotenv>=1.0,<2.0

# --- Vendor integration -----------------------------------------------------
httpx>=0.27,<1.0

truststore>=0.9 ; sys_platform == "win32"
boto3>=1.34,<2.0

# --- Registry ---------------------------------------------------------------
sqlalchemy>=2.0,<3.0
pyodbc>=5.1,<6.0
alembic>=1.13,<2.0

# --- Validation ---------------------------------------------------------------
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# --- Control plane ----------------------------------------------------------
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
streamlit>=1.36,<2.0

# --- Observability ----------------------------------------------------------
structlog>=24.1

# --- CLI --------------------------------------------------------------------
typer>=0.12,<1.0
