# AMIGO -- published runtime

Generated by `scripts/publish/push_amigo_to_demo.py`. **Do not edit here.**

This is the stripped runtime copy of AMIGO, published for a client machine
that cannot pull from the master repository. Comments and docstrings have
been removed; tests, documentation and development tooling are not included.

- `VERSION.txt` -- the commit this copy was built from.
- `MANIFEST.txt` -- size, line count and SHA-256 of every file.
- `scripts/check_copy.py` -- verifies this copy against the manifest.
  Use `--all` on a first setup; it skips config files by default.

Master is the AML-MigHub repository. A fix made here is overwritten by the
next publish, and made blind -- the comments explaining why the code is as
it is were stripped out. Every change goes into master and is republished.
